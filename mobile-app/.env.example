# Copia este archivo a `.env` y rellena con los MISMOS valores de la web
# (NEXT_PUBLIC_SUPABASE_URL / NEXT_PUBLIC_SUPABASE_ANON_KEY).
# La anon key es pública por diseño; la seguridad real la da la RLS de Supabase.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
